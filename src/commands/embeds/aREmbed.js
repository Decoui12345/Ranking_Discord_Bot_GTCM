/**const { EmbedBuilder } = require('discord.js');
 
const aREmbed = new EmbedBuilder()
.setTitle('Who was added into the ranks.')
.setAuthor(`<@${interaction.user}>`)
// ^ ranker who used slash command
.setDescription(`<@${user}> was added into ${role}`)
.setTimestamp()**/